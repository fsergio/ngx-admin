export class Dictionary {
  constructor(public id?: string,
              public source?: string,
              public codeDXP?: string,
              public codeLegacy?: string,
              public transformValue?: string) {
  }
}
